name: pdf_contract_masking
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch=2.2
  - cpuonly
  - pip:
    - -e .
    - transformers>=4.30.0
    - tqdm
    - PyMuPDF
    - regex
    - sentencepiece
    - tokenizers
